{
  "name": "@satoshi-bets/contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "check": "clarinet check",
    "deploy:mainnet": "tsx scripts/deploy-mainnet.ts",
    "clean": "rm -rf .clarinet coverage"
  },
  "devDependencies": {
    "@hirosystems/clarinet-sdk": "^2.8.0",
    "@stacks/network": "^7.3.1",
    "@stacks/transactions": "^7.3.1",
    "dotenv": "^16.4.0",
    "tsx": "^4.19.0",
    "vitest": "^2.1.0",
    "typescript": "^5.7.0",
    "@vitest/coverage-istanbul": "^2.1.0"
  }
}
